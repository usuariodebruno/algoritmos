/* crescimentoPopulacional */

#include<stdio.h>

int num, pa, pb;
float g1, g2;
scanf("%d", &num);

for (int i = 0; i < num; i++){
    scanf("%d %d %f %f", &pa, &pb, &g1, &g2);
    if (pa < pb && pb =< 1000000) {
        
    }
}
